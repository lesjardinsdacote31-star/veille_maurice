# Clé PUBLIQUE uniquement (publishable/anon) — jamais la clé secrète ici,
# ce fichier finit dans le bundle JS envoyé au navigateur.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=

# Notifications Web Push : la clé publique générée par
# python -m scripts.generer_cles_vapid (une seule ligne, publique).
VITE_VAPID_PUBLIC_KEY=
